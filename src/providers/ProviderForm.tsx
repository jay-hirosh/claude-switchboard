// Placeholder — replaced in Task 8 with the real preset-driven form.
export function ProviderForm(_: {
  providerId: string | null;
  onClose: () => void;
  onSaved: () => void | Promise<void>;
}) {
  return null;
}
