# Feature roadmap — live awareness, guidance, outward surfaces

**Status:** 5 of 11 tracked items shipped (2026-08-12/13) — Phase 1 (F1–F3) plus both backlog items are on `main`. Phases 2–3 (F4–F9) remain planning-only; each still gets its own spec → plan → implement cycle when picked up.
**Date:** 2026-08-12 (created) / 2026-08-13 (status updated)

Nine features originally agreed for future implementation, sequenced by shared infrastructure and dependency. This is the decomposition document: it fixes scope, build order, and the architecture each feature hangs off, so individual specs can be written without re-deriving the big picture.

Backlog items alongside these:
- **Best-account indicator** — ✅ **Shipped.** Spec: `specs/2026-08-12-best-account-indicator-design.md`. Plan: `plans/2026-08-12-best-account-indicator.md`.
- **PAYG budget alerts** (burn-rate $ forecast + independent PAYG threshold) — ✅ **Shipped.** Spec: `specs/2026-08-12-payg-budget-alerts-design.md`. Plan: `plans/2026-08-12-payg-budget-alerts.md`. Final review caught and fixed a bug where the forecast could go negative right after a credits reset or top-up.

## Existing infrastructure these build on

| Capability | Where | Used by |
|---|---|---|
| Live JSONL watcher, emits `session_ingested` to frontend | `src-tauri/src/jsonl_parser/watcher.rs`, `lib.rs:672` | F1, F2, F3 |
| Running-process detection (CLI count, VS Code workspaces) | `src-tauri/src/process_detection.rs` | F1, F2 |
| Full usage-snapshot history per account, time-indexed | `api_snapshots` table | F5, F9 |
| Session event history (timestamps, models, tokens, cost) | `session_events` table | F1, F5, F6 |
| Context-window resolution per model | `src/sessions/contextWindow.ts` | F3 |
| Notifier rules engine + once-per-window dedup | `src-tauri/src/notifier/` | F2, F3, F4 |
| Statusline daemon snapshot *reading* | `poll_loop.rs::read_shared_snapshot` | F7 |
| Warm-up scheduler + presets | `src-tauri/src/scheduler/` | F6, F9 |
| WidgetKit host (tasks 1–3/6 done, on unmerged `widget` branch) | `native/macos/SwitchboardWidgetHost` | F8 |

## Phase 1 — Live session engine (F1 → F2 → F3) — ✅ Shipped

The shared core is a **live session registry** (`src-tauri/src/live_sessions.rs`): a backend module that correlates the file watcher's per-touch signal with actively-growing JSONL transcripts to answer "which sessions are running right now, and what have they consumed so far this session." Built once for F1; F2 and F3 turned out to be thin rules on top, exactly as planned.

### F1. "Now running" row — ✅ Shipped
Spec: `specs/2026-08-12-live-sessions-f1-design.md`. Plan: `plans/2026-08-12-live-sessions-f1.md`.
Popover section showing each live session: project name, model, cost, elapsed time — capped at 3 rows + "N more". Liveness resolved to Live (touched within 120s) → Cooling (120–300s) → removed (300s+), started strictly from watcher-driven ingest so the startup backfill never populates it. Final review caught and fixed two real bugs before merge: the popover had no content-height measurement at all and would clip its own footer the instant a session went live, and the subagent-transcript folding logic used a forward-slash string search that silently breaks on Windows.

### F2. Session-finished notifications — ✅ Shipped
Spec: `specs/2026-08-12-session-finished-f2-design.md`. Plan: `plans/2026-08-12-session-finished-f2.md`.
Native notification when a session that ran ≥10 minutes goes quiet for good (F1's Cooling→removed transition). Settings toggle, default on. Final review caught and fixed a dedup bug that would have permanently blocked a legitimate second notification for a session resumed after departing.

### F3. Context-window warning — ✅ Shipped
Spec: `specs/2026-08-12-context-warning-f3-design.md`. Plan: `plans/2026-08-12-context-warning-f3.md`.
Notification plus an amber popover chip when a live session's context crosses 80% of its window (hysteresis re-arms below 70%). Final review caught and fixed a Critical bug: the original 200K fallback for unrecognized models was factually wrong for this app's own shipped provider presets — GLM's real window is 1M (so it false-alarmed at 16% of the real window) and DeepSeek's real window is 131K (so the warning could never fire before real compaction). Fixed to skip the warning entirely rather than guess, matching the frontend's existing null-safe philosophy.

**Known, deliberately-parked limitation carried across all three:** liveness/"still working" detection is keyed to new *costed* transcript events, not every raw write — a session mid-way through a long tool call (a slow build, a long test suite) writes nothing for the duration, so it can depart and re-register with a fresh notification cycle. Surfaced independently in F1's, F2's, and F3's own final reviews; deserves one real design pass rather than a third patch. Not fixed on any of these three branches.

## Phase 2 — Guidance from history (F4, F5 → F6)

### F4. Model routing advisor (S)
One-line hint in the popover when per-model 7D buckets diverge sharply: *"Opus 7D at 82%, Sonnet at 31% — consider `/model sonnet`."*
- Pure frontend: `seven_day_opus` / `seven_day_sonnet` are already in every `CachedUsage`. A pure function (like `bestAccount.ts`) decides when the gap is actionable; renders as a dismissible hint row.
- Only renders on plans that report the per-model split (Pro/Team-Pro return null — same guard as the existing per-model bars).
- **Design questions:** gap threshold; dismiss-until-reset vs dismiss-forever.

### F5. Limit-hit analytics (M)
Report tab (or Trends section) answering "when do I actually run out?": count of times each bucket hit ≥95–100%, time-of-day distribution, which projects consumed the window beforehand.
- **Backend:** mine `api_snapshots` (full payload history already stored per account) for utilization peaks per window cycle; join window-start times against `session_events` to attribute consumption. New read-only IPC command; no schema change.
- **Design questions:** where it lives (new tab vs Trends section); how far back (30/90 days); per-account or active-account-only.

### F6. Smart warm-up suggestions (S — after F5)
Suggest warm-up anchor times from observed behavior: *"You usually start around 08:40 — anchor warm-up at 08:30?"*
- Reuses F5's history-mining: median first-event-of-day time from `session_events` (already timestamped). Surfaces as a suggestion chip in the warm-up `ScheduleSelector`, never auto-applies — warm-up stays strictly opt-in.
- **Design questions:** minimum history before suggesting (e.g. 10 active days); weekday/weekend split.

## Phase 3 — Outward surfaces (F7, F8, F9 — independent of each other)

### F8. Finish the macOS widget (M — resume existing work first)
Tasks 4–6 of the existing WidgetKit plan, on the unmerged `widget` branch (`native/macos/SwitchboardWidgetHost`). **Do early in Phase 3 or in parallel with Phase 2** — it's half-built, and unfinished branches rot as `main` moves. First step of its plan: rebase `widget` onto `main` and re-validate tasks 1–3.

### F9. Reset timeline view (M)
Horizontal timeline: every account's 5H/7D reset moments plus scheduled warm-up fires on one axis. Answers "account B frees up at 4pm, C at 6pm."
- Pure frontend over existing data (`resets_at` per bucket per account, warm-up schedules via `get_warmup_state`). Natural pairing with the best-account indicator — likely lives in the expanded report near the accounts sidebar.
- **Design questions:** time span (next 24h?), whether 7D resets (days out) share the axis or get a separate lane.

### F7. Statusline installer (M–L — most external-surface risk)
One-click install of a Switchboard-provided statusline command for Claude Code, showing 5H% in the terminal prompt. Inverse of the existing snapshot *reading* — Switchboard becomes the writer.
- Writes a `statusLine` entry to `~/.claude/settings.json` — must reuse the Providers feature's guarded-write machinery (backup + undo manifest + confirmation), same as the global-default-provider flow.
- **Design questions:** what the statusline binary/script actually is (shell script reading the shared snapshot vs a `switchboard statusline` CLI subcommand — the CLI already exists as `cli.rs`); coexistence with a user's existing statusline. Riskiest of the nine to get right; deliberately last.

## Build order

| # | Feature | Size | Depends on | Status |
|---|---|---|---|---|
| — | Best-account indicator | S | — | ✅ Shipped |
| — | PAYG budget alerts | S–M | — | ✅ Shipped |
| 1 | F1 Now-running row | M | — (builds live-session registry) | ✅ Shipped |
| 2 | F2 Agent-finished notifications | S | F1's registry | ✅ Shipped |
| 3 | F3 Context-window warning | S–M | F1's registry | ✅ Shipped |
| 4 | F8 Finish widget (parallel track) | M | `widget` branch rebase | Planned |
| 5 | F4 Model routing advisor | S | — | Planned |
| 6 | F5 Limit-hit analytics | M | — | Planned |
| 7 | F6 Smart warm-up suggestions | S | F5's history mining | Planned |
| 8 | F9 Reset timeline view | M | — (pairs with best-account indicator) | Planned |
| 9 | F7 Statusline installer | M–L | — (riskiest; last) | Planned |

Remaining follow-ups surfaced during Phase 1's final reviews, not yet scheduled:
- Exclude same-org accounts from the best-account indicator (they share quota — a recommendation there is a no-op).
- Fix the pre-existing `theme.test.ts` collection failure (unrelated to any of the above, makes `pnpm test` exit non-zero despite all tests passing).
- Vitest scans `.claude/worktrees/**` copies if a worktree is left around during a test run — add to the vitest `exclude` config.
- One real design pass on the "still working vs. actually finished" liveness signal (see Phase 1 note above) before F4–F9 build anything else on top of the same registry.

## Process

Each feature, when picked up: brainstorm its open design questions (listed per feature above) → spec in `specs/` → implementation plan in `plans/` → implement. No feature starts implementation off this document alone.
